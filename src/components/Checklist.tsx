import { useState } from 'react'
export function Checklist({ title, items }: { title: string; items: string[] }) { const [checked, setChecked] = useState<boolean[]>(items.map(() => false)); const reset = () => setChecked(items.map(() => false)); return <section className="checklist"><div className="checklist-head"><div><p className="eyebrow">CHECKLIST</p><h3>{title}</h3></div><button onClick={reset} aria-label="Reiniciar checklist">↻</button></div>{items.map((item, index) => <label key={item} className={checked[index] ? 'checked' : ''}><input type="checkbox" checked={checked[index]} onChange={() => setChecked((state) => state.map((value, i) => i === index ? !value : value))} /><span>{item}</span></label>)}<small>{checked.filter(Boolean).length} de {items.length} elementos</small></section> }
